spring.application.name=quiz-service

spring.datasource.url = jdbc:mysql://localhost:3306/quizDb
spring.datasource.username = root
spring.datasource.password = S$nEEE053
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.mysql.console.enabled=true
#this is for spring data jpa config
#update is for creating new table if there is not table, and if there is a table then update the table
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

#spring.sql.init.mode=always
#spring.sql.init.platform=mysql
#spring.jpa.defer-datasource-initialization=true